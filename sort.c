#include<stdio.h>
#include<stdlib.h>

int selectionsort(int *rarr, int size)
{}
int main(void)
{
int n,i;
int *arr;
printf("\nHow many nos. to be sorted?\n\n");
scanf("%d",&n);
arr=malloc(n*sizeof(int));
printf("Enter the nos. : \n ");

for(i=0;i<n;i++)
{
scanf("%d",&arr[i]);
}
switch(ch)
{
case 'A':
selectionSort(arr,n);
break;
default:
printf("wrong Option");
}
}
